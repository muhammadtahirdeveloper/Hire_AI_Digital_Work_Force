web: uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000}
worker: celery -A scheduler.celery_app worker --loglevel=info --concurrency=2 -Q default,agent,followups,reports
beat: celery -A scheduler.celery_app beat --loglevel=info
